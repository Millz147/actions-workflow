name: Hello World
on: 
  push:
    branches:
      - main

jobs:
  greetings:
    runs-on: ubuntu-latest
      steps:
        - name: message
          run: echo "Hello World.!!!"
        - name: nodeversion
          run: |
            node -v
            npm -v
